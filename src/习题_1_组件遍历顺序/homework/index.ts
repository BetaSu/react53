/**
 * 使用深度优先遍历实现一个遍历器
 * @param {React.ReactElement} Cpn
 * @param {(jsx: React.ReactElement) => void} callback
 * 编写完成后执行 npm test 1 跑用例
 */
export default function reconciler(Cpn: React.ReactElement): string[] {
  return [];
}

function beginWork() {
  
}

function completeWork() {
  
}
